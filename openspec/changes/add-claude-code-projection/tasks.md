## Prerequisites

This change requires `add-hierarchical-skills` to be implemented first.

## 1. Add claude-code as a built-in target

- [ ] 1.1 Detect `target == "claude-code"` in the strategy dispatch in
        `src/commands/sync.rs` before the generic strategy match
- [ ] 1.2 Implement `execute_claude_code` in `src/sync_core.rs`:
        scan `skills/<category>/<action>/SKILL.md` for all hierarchical skills;
        for each, parse frontmatter and write translated output to
        `.claude/commands/<category>/<action>.md`
- [ ] 1.3 Create all parent directories (`mkdir -p`) before writing each target file

## 2. Implement frontmatter translation

- [ ] 2.1 Parse wai SKILL.md frontmatter fields: `name`, `description`
- [ ] 2.2 Generate Claude Code frontmatter: `name` (formatted as "Category: Action"),
        `description`, `category` (from the path segment), retain body content unchanged
- [ ] 2.3 Add `# Auto-generated by wai — do not edit directly` header

## 3. Tests

- [ ] 3.1 Unit test for frontmatter translation: wai fields → Claude Code fields
- [ ] 3.2 Integration test: create `issue/gather` skill, run `wai sync` with
        `target: claude-code`, verify `.claude/commands/issue/gather.md` is created
        with correct frontmatter
- [ ] 3.3 Test that unknown target names still fall through to the existing strategy
        dispatch (no regression)

## 4. Documentation

- [ ] 4.1 Update `wai sync --help` to mention `claude-code` as a recognised built-in target
- [ ] 4.2 Document the frontmatter translation rule (capitalise segments, join with `: `)
        in the help text or inline comment
