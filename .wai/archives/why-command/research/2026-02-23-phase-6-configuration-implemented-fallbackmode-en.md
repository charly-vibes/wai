Phase 6 Configuration implemented. FallbackMode enum + fallback_mode() helper added to why.rs to support fallback='error' vs 'search'. is_external_backend(), privacy_notice_needed(), show_privacy_notice(), mark_privacy_notice_shown() added for one-time Claude API privacy notice with config persistence. WaiError::LlmNotAvailable added to error.rs for fallback=error case. 13 new tests pass. 6.1-6.3 were already done; 6.4-6.6 implemented here.
