# Local-only files
*.local
*.cache
# Active pipeline run pointers (like .git/HEAD — written by `wai pipeline start`,
# cleared by `wai close` when the run completes). Runtime-only, not committed.
.pipeline-run
.last-run
