Phase 9.1 complete: integrated -v/-vv/-vvv verbosity into wai why. -v shows elapsed time. -vv adds prompt/response char counts, estimated tokens (chars/4 heuristic), and estimated cost for Claude models (haiku/sonnet/opus pricing). -vvv adds full prompt dump. Added model_id() to LlmClient trait so cost estimation uses actual model string. Added estimate_cost() helper in llm.rs. 5 new tests for verbose_stats_lines, 5 for estimate_cost. All 46 lib + 135 integration tests pass. Skipped 9.2 streaming, 9.3 telemetry, 9.4 caching as decided optional.
