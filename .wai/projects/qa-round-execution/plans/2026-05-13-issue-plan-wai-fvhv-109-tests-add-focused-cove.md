---
tags: [pipeline-run:epic-autonomy-tdd-ro5-2026-05-13-work-one-ready-child-issue-from-epic-wai-fvhv, pipeline-step:plan]
---

ISSUE PLAN: wai-fvhv.109 — Tests: add focused coverage for wai pipeline authoring and integrity commands. Desired outcome: extend integration coverage for pipeline definition discovery and integrity flows by adding focused tests for list (non-empty/invalid signal if needed), show, validate, and at least one lock/verify success plus one failure path such as invalid TOML or hash mismatch. Out of scope: changing pipeline runtime behavior unless a test exposes a clear bug that is small enough to fix within this ticket; broad refactors of src/commands/pipeline.rs; docs updates. Exact files likely touched: tests/integration.rs primarily; src/commands/pipeline.rs only if a narrow bug fix is required to satisfy intended behavior. Exact commands to run: cargo test --test integration pipeline_; narrower loops around new tests with cargo test --test integration pipeline_(list|show|validate|lock|verify) when iterating; cargo fmt if test file changes. Work type: code/test work. Red/green/refactor: RED add failing integration tests for uncovered surfaces; GREEN implement the smallest fix only if tests reveal missing behavior, otherwise keep changes test-only; REFACTOR tidy helper duplication only if clearly local. Follow-up threshold: if integrity coverage requires new helper infrastructure or reveals semantics larger than one session, create a child follow-up under wai-fvhv instead of broadening this ticket.
